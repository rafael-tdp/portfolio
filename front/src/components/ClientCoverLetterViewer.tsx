"use client";

import React from "react";
import CoverLetterViewer from "./CoverLetterViewer";

export default function ClientCoverLetterViewer(props: any) {
  return <CoverLetterViewer {...props} />;
}
